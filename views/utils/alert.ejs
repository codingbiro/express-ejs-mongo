<% if (typeof registered !== 'undefined') { %> 
  <div class="uk-alert-success" uk-alert>
    <a class="uk-alert-close" uk-close></a>
    <p>Successful registration. Now you can log in.</p>
  </div>
<% } %>
<% if (typeof dateExpired !== 'undefined') { %> 
  <div class="uk-alert-danger" uk-alert>
    <a class="uk-alert-close" uk-close></a>
    <p>Select an upcoming date.</p>
  </div>
<% } %>
<% if (typeof lessonAdded !== 'undefined') { %> 
  <div class="uk-alert-success" uk-alert>
    <a class="uk-alert-close" uk-close></a>
    <p>Lesson has been added.</p>
  </div>
<% } %>
<% if (typeof deleted !== 'undefined') { %> 
  <div class="uk-alert-danger" uk-alert>
    <a class="uk-alert-close" uk-close></a>
    <p>Lesson has been deleted.</p>
  </div>
<% } %>

a<%= passwordsDoNotMatch%>